prototype(Onedrop.Form.Hubspot:Component.Atom.Email) < prototype(Onedrop.Form.Hubspot:Component.Atom.DefaultFormElement) {

    wrapperClass = 'input'
    isRequired = true

    renderer = afx`
        <input
            type="email"
            id={props.id}
            name={props.name}
            value={props.value}
            required={props.isRequired}
            autocomplete={props.properties.autocomplete ? props.properties.autocomplete : false}
        />
    `
}
